Day-04[28-05-2022]:
===================
	
	Registration Process:
	=====================
	http://engineering.apssdc.in/register/

	urls => views
	urls => views => template
	urls => views => template => views => template

	student
		=> date
		=> Title
		=> description 


	from appname.model import classname

	d = classname(field="value")
	d.save()

	k = classname.objects.create(field="value")
